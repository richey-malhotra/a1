import os

# Setting DEVELOPMENT turns DEBUG on for local work.
os.environ.setdefault('DEVELOPMENT', '1')

# Your own secret key. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(50))"
os.environ.setdefault('SECRET_KEY', 'replace-with-your-own-secret-key')
